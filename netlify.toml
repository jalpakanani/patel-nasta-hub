# Next.js static export (`output: "export"`)
# Netlify env: NEXT_PUBLIC_GA_MEASUREMENT_ID, NEXT_PUBLIC_ADSENSE_CLIENT_ID=ca-pub-…, NEXT_PUBLIC_ADSENSE_SLOT_HOME (optional)
[build]
  command = "npm run build"
  publish = "out"
